# ============================================================
# Nginx Configuration for Travel Agent API
# Location: /etc/nginx/sites-available/travel-agent
# ============================================================

server {
    listen 80;
    listen [::]:80;
    server_name api.100cr.cloud;

    # Redirect HTTP to HTTPS (uncomment after SSL is configured)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_http_version 1.1;
        
        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support (if needed)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Timeouts for long-running requests (AI model loading)
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
        
        # CORS headers (handled by Flask, but backup)
        add_header Access-Control-Allow-Origin "https://100cr.cloud" always;
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type, Authorization" always;
        
        # Handle preflight OPTIONS requests
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "https://100cr.cloud";
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
            add_header Access-Control-Allow-Headers "Content-Type, Authorization";
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type "text/plain; charset=utf-8";
            add_header Content-Length 0;
            return 204;
        }
    }
    
    # Health check endpoint
    location /health {
        proxy_pass http://127.0.0.1:5001/api/warmup;
        access_log off;
    }
}

# HTTPS server block (will be auto-generated by Certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name api.100cr.cloud;
#     
#     ssl_certificate /etc/letsencrypt/live/api.100cr.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.100cr.cloud/privkey.pem;
#     
#     # ... same location blocks as above ...
# }
